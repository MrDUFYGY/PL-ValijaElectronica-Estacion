---
import "../styles/global.css";
import ContenidoAMostrar from "../components/ContenidoAMostrar.astro";
const { content, station, date, title, idsSubprocesos = [], otro, IdProcesoDia } = Astro.props;
import ScrollPanelButtons from "../components/ScrollPanelButtons.astro";
import Huella from "../components/ConfirmHuella.astro";

console.log("IdProcesoDia", IdProcesoDia);


export const prerender = false;
// const user = Astro.locals.user;

// // 2. Validar si el usuario existe. Si no, redirigir al inicio.
// // Esta es una segunda capa de seguridad en la propia página.
// if (!user) {
//   return Astro.redirect('/');
// }

---


<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<div id="">
			<ContenidoAMostrar station={station} date={date} title={title}/>
			<ScrollPanelButtons
			scrollTargetId="button-scroll"
			scrollContainerId="navTop-scroll"/>
		</div>
		<main>
			<slot />
			
			<!-- <p>{otro}</p>
			<p>{idsSubprocesos}</p> -->
		</main>
		<br />
		<div class="flex items-center justify-center">
			<Huella idsSubprocesos={idsSubprocesos} IdProcesoDia={IdProcesoDia}/>
			<h1 id="subprocesos" data-ids={idsSubprocesos}>{idsSubprocesos}</h1>
		</div>
		<div id="button-scroll">
			
		</div>
	</body>
	<script>
		const h1 = document.getElementById('subprocesos');
		const ids = h1?.dataset.ids;
		console.log(ids);		
	</script>	
</html>

<style>
	main {
		margin: 0;
		padding: 1vw;
		width: 100%;
		height: 100%;
	}
	body {
		font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
		background-color: #2d2d2d;

	}
	*{
		scrollbar-color: white #2d2d2d;
		scrollbar-width: thin;
		scroll-behavior: smooth;
	}
</style>
