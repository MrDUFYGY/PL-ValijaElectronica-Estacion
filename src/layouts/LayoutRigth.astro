---
import "../styles/global.css";
import ContenidoAMostrar from "../components/ContenidoAMostrar.astro";
const { content, station, date, title } = Astro.props;
import Huella from "../components/ConfirmHuella.astro";

export const prerender = false;
// const user = Astro.locals.user;

// // 2. Validar si el usuario existe. Si no, redirigir al inicio.
// // Esta es una segunda capa de seguridad en la propia página.
// if (!user) {
//   return Astro.redirect('/');
// }

---


<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<ContenidoAMostrar station={station} date={date} title={title}/>
		<main>
			<slot />
		</main>
		<br />
		<div class="flex items-center justify-center">
			<Huella />
		</div>
	</body>
</html>

<style>
	main {
		margin: 0;
		padding: 1vw;
		width: 100%;
		height: 100%;
	}
	body {
		font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;


	}
	*{
		scrollbar-color: white #2d2d2d;
		scrollbar-width: thin;
		scroll-behavior: smooth;
	}
</style>
